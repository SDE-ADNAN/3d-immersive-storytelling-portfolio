"use client";

import { useEffect } from 'react';

export default function LiquidEffectAnimation() {
  useEffect(() => {
    // Placeholder for hero-only liquid effect (R3F / three.js will be added later).
    return () => {
      // cleanup placeholder
    };
  }, []);

  return null;
}
